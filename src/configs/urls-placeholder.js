const baseUrlPlaceholder = 'https://jsonplaceholder.typicode.com';

export const urlsPlaceholder = {
    users: '/users',
    posts: '/posts',
    comments: '/comments'
}

export default baseUrlPlaceholder;